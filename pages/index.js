import React from 'react';
import { Container, Typography } from '@mui/material';

const Home = () => {
    return (
        <Container>
            dfdf
        </Container>
    );
};

export default Home;
